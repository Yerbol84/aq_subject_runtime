name: aq_subject_runtime
description: Subject runtime implementation for AQ Platform
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  aq_schema:
    path: ../aq_schema
  logging: ^1.2.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
